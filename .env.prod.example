# =============================================================================
# Sentinel Production Environment
# =============================================================================
#
# IMPORTANT: This file contains secrets. Do NOT commit to version control.
#
# Usage:
#   1. Copy this file and fill in actual values
#   2. scp .env.prod deploy@your-vps:/tmp/
#   3. Run deploy script with --env-file /tmp/.env.prod
#
# =============================================================================

# -----------------------------------------------------------------------------
# Zion API Settings (REQUIRED)
# -----------------------------------------------------------------------------
# The Zion governance system for user authentication and rate limiting
ZION_API_URL=https://your-zion-api.com
ZION_API_KEY=your-zion-api-key-here

# -----------------------------------------------------------------------------
# OpenAI API Settings (REQUIRED)
# -----------------------------------------------------------------------------
# All AI traffic is routed through OpenAI
OPENAI_API_URL=https://api.openai.com/v1
OPENAI_API_KEY=sk-your-openai-api-key-here

# -----------------------------------------------------------------------------
# Server Settings
# -----------------------------------------------------------------------------
# These defaults are suitable for production
SENTINEL_HOST=0.0.0.0
SENTINEL_PORT=8080

# Production logging level (info is recommended, use debug for troubleshooting)
RUST_LOG=sentinel=info,tower_http=info

# -----------------------------------------------------------------------------
# Cache Settings
# -----------------------------------------------------------------------------
# User limits cache TTL (seconds)
CACHE_TTL_SECONDS=300

# JWT validation cache TTL (seconds)
JWT_CACHE_TTL_SECONDS=300

# -----------------------------------------------------------------------------
# Docker Settings (used by docker-compose)
# -----------------------------------------------------------------------------
# These are set by the deployment script's docker-compose.override.yml
# but you can override them here if needed

# External port for Sentinel container (default: 8080)
# SENTINEL_DOCKER_PORT=8080

# External port for Redis container (default: 6379)
# SENTINEL_REDIS_PORT=6379
