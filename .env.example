# =============================================================================
# Sentinel AI Proxy Configuration
# =============================================================================

# -----------------------------------------------------------------------------
# Server Settings
# -----------------------------------------------------------------------------
SENTINEL_HOST=0.0.0.0
SENTINEL_PORT=8080

# -----------------------------------------------------------------------------
# Logging
# -----------------------------------------------------------------------------
RUST_LOG=sentinel=debug,tower_http=debug

# -----------------------------------------------------------------------------
# Redis Connection
# -----------------------------------------------------------------------------
# For local development (running cargo directly):
REDIS_URL=redis://localhost:6379

# For Docker development (uses sentinel-redis container):
# REDIS_URL=redis://sentinel-redis:6379

# -----------------------------------------------------------------------------
# Zion API Settings
# -----------------------------------------------------------------------------
# For Zion running on host machine:
# ZION_API_URL=http://localhost:3000

# For Zion running in Docker (from Sentinel container):
# ZION_API_URL=http://host.docker.internal:3000

# For Zion in same Docker network:
# ZION_API_URL=http://zion-api:3000

ZION_API_URL=http://localhost:3000
ZION_API_KEY=your-api-key-here

# -----------------------------------------------------------------------------
# Vercel AI Gateway Settings
# -----------------------------------------------------------------------------
VERCEL_AI_GATEWAY_URL=https://api.vercel.ai/v1
VERCEL_AI_GATEWAY_API_KEY=your-vercel-key

# -----------------------------------------------------------------------------
# OpenAI Direct API Settings (for endpoints not supported by Vercel AI Gateway)
# Required for: /v1/responses (Responses API)
# -----------------------------------------------------------------------------
OPENAI_API_URL=https://api.openai.com/v1
OPENAI_API_KEY=your-openai-key

# -----------------------------------------------------------------------------
# Cache Settings
# -----------------------------------------------------------------------------
CACHE_TTL_SECONDS=300
JWT_CACHE_TTL_SECONDS=300

# -----------------------------------------------------------------------------
# Docker Compose Settings (only used by docker-compose.yml)
# -----------------------------------------------------------------------------
# External port for Sentinel (default: 8080)
# SENTINEL_PORT=8080

# External port for Redis (default: 6380 to avoid conflicts)
# SENTINEL_REDIS_PORT=6380

# To join an external Docker network (e.g., Zion's network):
# EXTERNAL_NETWORK=zion_default
